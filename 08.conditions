#!/bin/bash

echo "enter the number"
read NEMBER

if [ $NEMBER -gt 10 ]
then
    echo "given number $NEMBER is greater than 10"
else
    echo "given number $NEMBER is less than 10"
fi

#-gt greater than; -lt less than; -eq equls to; -ge greater than equls to; -le less than equls to
